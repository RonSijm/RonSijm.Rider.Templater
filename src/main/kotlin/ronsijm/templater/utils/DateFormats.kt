package ronsijm.templater.utils

/**
 * Centralized date format constants
 * Used across all date commands for consistency
 */
object DateFormats {
    const val DEFAULT_DATE = "yyyy-MM-dd"
    const val DEFAULT_DATETIME = "yyyy-MM-dd HH:mm:ss"
    const val DEFAULT_TIME = "HH:mm:ss"
}

